@using BlazorDemo.Client.Components
@using Kenova.Client.Components

<PortalApp AppAssembly="@typeof(Routes).Assembly" AdditionalAssemblies="new[] { typeof(SearchCustomers).Assembly, typeof(Portal).Assembly}" />

